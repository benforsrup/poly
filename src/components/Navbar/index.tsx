import { Navbar as MantineNavbar } from "@mantine/core";

export const Navbar = () => {
  return (
    <MantineNavbar width={{ base: 100 }} p="xs">
      {/* Navbar content */}
    </MantineNavbar>
  );
};
